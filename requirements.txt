# Core only — mirrors [project.dependencies] in pyproject.toml.
# For the embed stage (Stage 4) also install: pip install -e ".[embed]"
# See the README's GPU section before installing torch on Windows.
httpx>=0.27
tenacity>=9.0
pandas>=2.2
pyarrow>=16.0
numpy>=1.26
tqdm>=4.66
python-dotenv>=1.0
