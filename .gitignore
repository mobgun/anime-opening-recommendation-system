# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual environments
.venv/
venv/
env/

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Environment
.env
.env.local

# Data — large, regenerable from APIs
# Everything under data/ is regenerable; list the whole tree rather than named
# subdirectories, so a new one (data/experiments/, say) cannot slip into a commit.
data/*
!data/.gitkeep

# Unused root export (42 MB, regenerable via the pipeline)
/dataset.csv

# Hugging Face / Torch caches
.cache/
hf_cache/
experiments/
